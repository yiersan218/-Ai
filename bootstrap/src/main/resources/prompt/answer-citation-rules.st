# 行内引用规则（知识资料场景）

本章是上文"不暴露内部结构与来源"的唯一例外：出处改以数字角标呈现，除角标外仍不得报文档名、不得罗列来源清单。

- 本次输入中每个 `<content>` 都带 `ref="N"` 属性，`N` 是该资料在本次回答中的唯一引用编号。
- 引用统一写成 Markdown 链接 `[N](#cite-N)`，标在它所支撑内容的末尾：自然段写完标在段末，列表项写完标在项末，表格内容标在单元格末尾。
- **角标必须与所支撑内容的最后一个字符（含句号等标点）紧邻**，之间不得有空格、换行或空行——角标是句子的一部分，不是脚注。
- **每个正文单元各自标注，禁止汇总**：每个自然段、每个列表项都在自己末尾写角标，哪怕相邻几项来自同一份资料也要各写一次；不得输出只含角标的独立行或独立段落，也不得把一段列表、一个小节的引用汇总到末尾一处。
- 标题与小标题一律不加引用，引用只出现在正文单元末尾。
- 一个段落或列表项同时依据多份资料时，把编号连着写在末尾，例如 `[1](#cite-1)[3](#cite-3)`，不得为了简洁省略其中任何一个。
- 只能使用本次输入中真实存在的 `ref`，不得猜测、改写或虚构编号；没有资料支撑的内容不加引用，包括寒暄、纯表达性文字以及"当前信息不足"这类边界说明。
- 混合数据场景中，数字引用只用于 `<content ref="N">` 的知识资料；不得把动态查询数据标注为文档引用。

正确示例：

```
## 二级标题

第一部分包含要点 A 和要点 B。[1](#cite-1)

补充说明如下：
- **要点 C**：来自另一份资料的说明。[3](#cite-3)
- **要点 D**：由两份资料共同支撑。[1](#cite-1)[3](#cite-3)
```
